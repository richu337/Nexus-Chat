# ── Firebase Web Configuration ────────────────────────────────────────────
# Copy this file to `.env` and fill in your Firebase project values.
# Get these from Firebase Console → Project settings → General → Your apps.

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# Web Push (FCM) VAPID key — Firebase Console → Project settings → Cloud Messaging.
VITE_FIREBASE_VAPID_KEY=

# Optional: URL of the self-hosted notification relay (Node/Express backend).
# Leave empty to run without push notifications (app still works).
VITE_NOTIFICATION_RELAY_URL=

# URL of the OTA update manifest (served by the relay server at
# /update/latest.json). Leave empty to run without over-the-air updates.
VITE_UPDATE_MANIFEST_URL=

# ── Server-side configuration (server/.env) ────────────────────────────────
# These belong in server/.env, never in the frontend .env
# FIREBASE_PROJECT_ID=
# FIREBASE_CLIENT_EMAIL=
# FIREBASE_PRIVATE_KEY=
# PORT=3000
